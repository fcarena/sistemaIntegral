{% extends 'base.html.twig' %}


{% set activeMenu = "producto" %}

{% block breadcrumb %}
  <li><a href="#">Producto</a></li>
  <li class="active">Ver Paleta</li>
{% endblock %}

{% block sectionBody %}
    <h1>Raqueta</h1>

    <table>
        <tbody>
            <tr>
                <th>Identifier</th>
                <td>{{ raquetum.identifier }}</td>
            </tr>
            <tr>
                <th>Color</th>
                <td>{{ raquetum.color }}</td>
            </tr>
            <tr>
                <th>Estado</th>
                <td>{{ raquetum.estado }}</td>
            </tr>
            <tr>
                <th>Id</th>
                <td>{{ raquetum.id }}</td>
            </tr>
            <tr>
                <th>Createdat</th>
                <td>{% if raquetum.createdAt %}{{ raquetum.createdAt|date('Y-m-d H:i:s') }}{% endif %}</td>
            </tr>
            <tr>
                <th>Availability</th>
                <td>{% if raquetum.availability %}Yes{% else %}No{% endif %}</td>
            </tr>
            <tr>
                <th>Notes</th>
                <td>{{ raquetum.notes }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('producto_raqueta_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('producto_raqueta_edit', { 'id': raquetum.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
    {% endblock %}

    {% block javascripts %}
      <script src="https://maps.googleapis.com/maps/api/js?key=AIzaSyA3SbyFGlZKCWqTkydIlRHcUWPgPJyfqrA&callback=laFuentePrestamos.newUserPage.initGoogleMaps" async defer ></script>
      <script src="{{ asset('js/imageViewer.js') }}" type="text/javascript" charset="utf-8"></script>
      <script src="{{ asset('js/laFuente/pages/newUser.js') }}" type="text/javascript" charset="utf-8"></script>
    {% endblock %}
